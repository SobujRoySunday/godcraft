import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['yt3.ggpht.com', "lh3.googleusercontent.com"], // Add YouTube images domain here
  },
  /* other config options can go here */
};

export default nextConfig;
